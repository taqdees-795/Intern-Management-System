# Task 2 — Intern Management System

## Overview
Core admin dashboard for intern onboarding, task assignment by internship type, and progress tracking.

## Login
Use your InternHub credentials (created via Task 2 Admin dashboard).
- **Admin:** `admin@internhub.com` / `Admin@2024`
- **Intern:** Added by Admin
currently i have intern
'taqdees795@gmail.com' /'1234'

## Setup
```bash
# Backend
cd backend && npm install && npm run dev   # runs on :5002

# Frontend (new terminal)
cd frontend && npm install && npm start    # runs on :3000
```

## Database
- MongoDB: `intern_management` (shared across all tasks)
- JWT Secret: `InternHub_SharedSecret_2024` (shared)
- Port: `5002`
